version: '3.7'

services:

  app:
    image: markus621/golang:1.0.0
    container_name: arwos-server-appgo
    restart: always
    working_dir: /app
    ports:
      - 10021:10021
      - 10022:10022
    volumes:
      - ./../../:/app
